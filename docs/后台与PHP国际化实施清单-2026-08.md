# 后台与 PHP 国际化实施清单（2026-08）

## 2026-08-14 实施进度

- 第一批维护页面：已完成 PHP 默认文案、维护拦截提示、倒计时说明和独立倒计时 JS 的运行时翻译与 POT 提取。
- 第二批维护工具 REST/PHP：已完成数据库清理、媒体体检、WebP 批处理、SEO 检查和搜索增强的用户可见消息；REST/error code 保持不变。
- 第三批共享 PHP：已完成设置保存/回滚、权限与并发冲突、限流、评论校验、登录搜索、Cookie/微信默认文案和隐私说明；审计分类与评论拦截原因键保持稳定。
- 第四批元数据与后台前端：已完成模块元数据运行时翻译、功能搜索、导航外壳、保存差异、风险确认、维护样式和媒体选择器等公共表面；本阶段又完成维护设置、评论/权限/SEO/站点/媒体/搜索增强/对象存储等高频表单的主要 `label`、`extra`、`placeholder` 和状态提示。仪表盘、国内生态聚合页、关于/AI 诊断及少量旧表单仍需下一批清理，不能标记为全量完成。

### 2026-08-14 本阶段增量

- 已覆盖：`vite/admin/src/components/page/function.tsx`、`page/comment.tsx`、`page/jurisdiction.tsx`、`function/seo.tsx`、`function/index.tsx`、`optimize/admin.tsx`、`optimize/site.tsx`、`optimize/medium.tsx`、`performance/search_enhance.tsx`、`performance/oss.tsx`，以及设置页签、风险提示、预览面板、凭据字段、保存/差异/选项校验工具。
- 动态文案统一使用完整格式串；状态标签的中文键仍作为稳定内部协议值，由 `StatusTag` 显示层翻译。
- 验收：Admin Vitest 48 files / 253 tests、PHPUnit 629 tests / 246360 assertions、PHPStan 0 errors、Admin/Count 生产构建、链接检查、`composer i18n:pot`、`git diff --check` 通过。
- Local 只读冒烟：WordPress 7.0.4 中概览、存储与维护、评论设置正常加载，页面无控制台错误，未执行保存、连接测试或维护操作。
- 精确发布包：230 entries，1,123,201 bytes，SHA-256 `a1de1dfb2146af4cc3d61f4f0ad1720a0c1fa698615cf5ed2d199df39ecfb5bf`；Docker 当时未运行，因此该 ZIP 的干净安装和 Plugin Check 仍待下一阶段，旧 ZIP 的扫描结论不得沿用。
- 下一批：仪表盘、国内生态聚合页及合规/评论安全抽屉、关于/AI 诊断运行状态、剩余旧表单和少量错误提示。

POT 构建已增加 `bin/export-i18n-metadata.php`，从模块 Registry、生成设置契约、隐私数据及生产后台 TS/TSX 中收集中文源文案，再与 PHP 和维护倒计时脚本文案合并。该目录只负责确保源文案可交付给译者；运行时仍必须在显示边界调用 WordPress `__()`。

## 状态定义

- **已覆盖**：文案经过 WordPress 翻译函数或后台 `wp-i18n` helper，且关键 `msgid` 已进入 POT 并有运行时测试。
- **待实施**：用户可见，但当前仍是硬编码、动态字符串未提取，或缺少运行时/契约验证。
- **保留原文**：用户输入、机器 ID、审计分类、数据库键、第三方品牌名等不应被翻译；如在界面显示，必须在显示层决定是否翻译。
- **待决策**：同一数据既用于 UI 又用于 AI/审计协议，需要先确定显示语言和稳定协议边界。

## 第一批：维护页面（P0）

| 表面 | 当前文件 | 问题 | 处理方式 |
|---|---|---|---|
| 默认维护标题、正文 | `admin/partials/page/function/maintenance/index.php` | 默认 PHP 文案硬编码 | `__()`/`esc_html__()`，用户自定义标题和正文不翻译 |
| 维护提示拦截信息 | `admin/partials/page/function/maintenance_tips.php` | `wp_die()` 文案硬编码 | 使用带文本域的 `wp_die()` 文案 |
| 红色维护模板 | `admin/partials/page/function/maintenance/red.php` | 倒计时说明来自模板 | 使用 PHP 翻译后的数据，不在模板中拼语言片段 |
| 倒计时说明 | `admin/partials/page/function/maintenance/countdown/index.php` | PHP 模板硬编码 | `esc_html__()` |
| 天/时/分/秒/结束 | `admin/partials/page/function/maintenance/countdown/main.js` | 独立前台 JS 未接入 `wp-i18n` | 依赖 `wp-i18n`，使用带 domain 的 `__()`，扩展 POT 提取 |

## 第二批：维护工具 REST/PHP（P0/P1）

| 模块 | 主要文件 | 待处理内容 |
|---|---|---|
| 数据库清理 | `admin/partials/performance/db_clean/index.php` | 权限、参数、预览失效、执行结果、计划周期、数量单位 |
| 媒体体检 | `admin/partials/performance/media_health/index.php` | 权限、问题类型、抽样数量、Alt/特色图等运行时标签 |
| WebP 批处理 | `admin/partials/performance/media_health/webp_batch.php` | 转换、跳过、恢复、文件/数据库失败信息和动态文件名 |
| SEO 检查 | `admin/partials/performance/seo_checker/index.php` | 问题类别、数量和建议文本 |
| 搜索增强 | `admin/partials/performance/search_enhance/index.php` | 前台无结果提示 |

所有 REST/PHP 错误必须保留稳定 error code，仅翻译 `message`；动态文案必须使用完整格式串和 `sprintf()`，不能拆成中文片段。

## 第三批：共享 PHP 用户路径（P1）

- `admin/class-npcink-toolbox-admin.php`：保存、回滚、并发冲突和权限错误。
- `includes/class-npcink-toolbox-config-manager.php`：配置结构、敏感字段和保存失败错误。
- `includes/class-npcink-toolbox-rate-limiter.php`：限流和安全验证提示。
- `admin/partials/domestic/comment_security/index.php`：评论拦截的 `wp_die()` 提示。
- `admin/partials/page/comment/*.php`：登录、重复评论、评论频率和评论内容校验。
- `admin/partials/privacy/index.php`：隐私提示和外部服务说明数据。

审计日志、数据库 option key、REST error code 和诊断事实 ID 保持稳定；管理界面展示这些值时再进行翻译。

## 第四批：模块元数据与后台前端（P1/P2）

- `admin/modules/registry.php` 的 `label`、`group`、`risk_tags`。
- 由模块元数据生成的设置契约、功能搜索索引和风险确认标题。
- `vite/admin/src/components/tab.tsx`、`vite/admin/src/components/feature-search.tsx`、`vite/admin/src/components/diff-modal.tsx`、`vite/admin/src/tool/riskyFeature.tsx` 的导航、无障碍标签和状态文案。
- 图片选择器、维护样式选择器、权限/SEO/站点优化表单的静态标签与提示。
- 关于/运行状态/AI 诊断中用户可见的模式名、报告标题、事实标签和限制说明。

元数据不能只在每个组件中临时翻译。应统一规定：稳定 ID 由 PHP/契约提供，显示层通过同一 `__()` helper 翻译；生成文件需要同时具备可抽取的源文案和契约测试。

## 不应直接翻译的内容

- 用户在设置中填写的标题、正文、维护提示和自定义 CDN 规则；
- REST error code、设置路径、模块 ID、数据库键、审计分类和事实 ID；
- 第三方服务名、域名、Bucket、Endpoint、PHP/WordPress 版本号；
- 仅供 AI 运行时使用的稳定协议字段。AI prompt 是否按 locale 生成，另立契约，不把 UI 翻译片段直接拼入协议。

## 每批完成条件

1. 生产 PHP/TS/TSX/独立 JS 文案已分类，没有新增未登记的用户可见硬编码。
2. `composer i18n:pot` 后抽查关键 `msgid`，并由契约测试锁定。
3. 中文源语言和至少一个非中文 WordPress locale 在真实运行时完成页面/REST 冒烟。
4. 动态占位符可被译者重排，REST code、设置结构和审计数据不变。
5. 若触及发布包，重新构建精确 ZIP；旧 ZIP 和旧 Plugin Check 结论不再作为证据。
